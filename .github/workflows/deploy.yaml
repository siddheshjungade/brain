name: Build Quartz Site

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@latest

      - name: Setup Pages
        uses: actions/configure-pages@latest

      - name: Build
        uses: konstfish/quartz-build-action@latest
        with:
          source: docs
          page_title: "Sid's Brain"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@latest
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4